<%= form_for @dice, :html => {:class => 'form-horizontal' } do |form| %>
  <% if @dice.errors.present? %>
    <ul>
      <% @dice.errors.full_messages.each do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  <% end %>
  
  <fieldset>
    <div class="control-group">
      <%= form.label :sides, :class => 'control-label' %>
      <div class="controls">
        <%= form.text_field :sides %>
      </div>
    </div>
  </fieldset>
  
  <fieldset class="form-actions">
    <%= form.submit "Create", :class => 'btn btn-primary' %>
  </fieldset>
<% end %>